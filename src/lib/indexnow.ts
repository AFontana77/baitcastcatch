/**
 * IndexNow key for baitcastcatch.com.
 *
 * The matching key file is served at /8005a28e0ed81e78af74835316d601e6.txt from public/. Bing verifies the
 * key by fetching that file, so the two must stay in step. One key per property:
 * a leaked key cannot then be used to submit URLs for a different site.
 */
export const INDEXNOW_KEY = "8005a28e0ed81e78af74835316d601e6";
